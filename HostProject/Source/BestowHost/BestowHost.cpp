// Minimal runtime module: the host project exists only to compile and
// self-test the BestowExport plugin.

#include "Modules/ModuleManager.h"

IMPLEMENT_PRIMARY_GAME_MODULE(FDefaultGameModuleImpl, BestowHost, "BestowHost");
